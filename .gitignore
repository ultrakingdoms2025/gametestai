node_modules/
dist/
.vite/

# Secrets - never commit
.env
.env.local
.env.*.local

# Automated visual-review output
review/
*.log

# the site is its own Vercel project; its build output and deps stay local
site/node_modules
site/.next
site/.vercel

.playwright-mcp/

# Superpowers brainstorming scratch
.superpowers/

# Station audit reports - regenerated by scripts/station-audit.mjs, megabytes each
output/

# Chrome profiles left by CDP measurement harnesses. Never source.
.chrome-harness-profile/

# Personal Claude Code overrides (permissions etc). Must never be published.
.claude/settings.local.json

# Agent scratch probes - never commit
.probe/

# Desktop release output - upload these to Steam/Partner Center instead of
# committing unpacked Electron runtimes to the source repository.
PCWINDOWSTEAMWRAPPER/
desktop/release/
desktop/node_modules/

claude-skills-threejs-ecs-ts/
dray-bow.txt

# Transient per-agent worktrees created by the harness. Never tracked: they
# live inside the repo, so an untracked one can make git resolve a new
# worktree against the checkout above it.
.claude/worktrees/
