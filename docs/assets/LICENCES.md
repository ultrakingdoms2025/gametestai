# Asset licence ledger

Every external or authored file under `public/assets/` gets a line here on the
day it lands, whether or not its licence carries an attribution obligation.
`scripts/tests/maze-assets.test.mjs` enforces both halves: every manifest
entry's licence must come from the allow-list below, and every entry's id must
appear in this file.

## Allow-list

| Licence | Obligation |
|---|---|
| `CC0-1.0` | None. Preferred for anything fetched from outside. |
| `CC-BY-4.0` | Attribution REQUIRED - the line here must name the author and source URL. |
| `proprietary-owned` | None - the project owns it outright. |
| `generated` | None - produced by a script in this repository (or a generator whose terms are recorded on its line). |

Anything else - Sketchfab's per-model terms especially - is not accepted. If a
model is worth an exception, the exception goes through the allow-list in the
test, deliberately, in its own commit.

## Texture sets

External surface textures are made by `scripts/make-world-tex.mjs`. It fetches
a CC0 set from ambientCG or Poly Haven into a cache, packs ORM to the glTF
convention (R=AO, G=roughness, B=metalness), compresses to KTX2 with the wasm
Basis encoder - no native toolchain, so it runs wherever node runs - and
PRINTS both the manifest entries and the ledger row for what it made. The line
below therefore lands in the same run as the file it describes, which is the
only arrangement under which a ledger stays complete.

The maze's five surfaces predate that script: they were made by hand on
2026-08-10 with toktx v4.4.2, on a machine whose toolchain is not in this
repository and is not on any machine that has built the game since. Nothing
was lost, because a KTX2 file records the command line that wrote it.
`KTXwriterScParams` in all fifteen recovers the pass exactly - `--encode etc1s
--qlevel 192 --clevel 2` for albedo, `--qlevel 160` for ORM, and `--encode
uastc --uastc_quality 2 --zcmp 19` for the normal maps, UASTC there because
ETC1S bands on the shallow gradients a normal map is mostly made of. That
recipe is now `ENCODE_PROFILE` in the script, pinned against those committed
bytes by `scripts/tests/world-tex-pipeline.test.mjs`. Re-running the pipeline
over the same sources reproduces the set to within +2.7% of its 17.55 MB, and
the difference is one lossless zstd setting the wasm encoder does not expose.

## Ledger

| id | file | licence | source | fetched/made |
|---|---|---|---|---|
| `raider` | `public/assets/npc/raider.glb` | `generated` | `scripts/make-npc-glb.mjs` - authored hero features for the station's four attacker archetypes: crested cranium, brow shelf, open muzzle with fangs, ears, glowing eye caps, pauldron, bandolier, dorsal and forearm spikes, belt and knuckled hands. Built from three.js primitives by this repository's own script against the reference sheets in `demopics/` (`g1`-`g4`). No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/npc-assets.test.mjs` asserts. | 2026-08-23 |
| `crew` | `public/assets/npc/crew.glb` | `generated` | `scripts/make-npc-glb.mjs` - authored hero features for the station's seven fixed interactive roles: gorilla cranium, brow, closed muzzle, ears, EVA collar ring, life-support pack, chest rig with a lit panel, belt, boots and bare knuckled hands. Same script, same provenance, authored against `demopics/n1`-`n7`. The helmet bubble those references wear is deliberately absent: a transparent material is a new shader-program family, and this pipeline exists to avoid exactly that. | 2026-08-23 |
| `wolf` | `public/assets/medieval/wolf.glb` | `generated` | `scripts/make-beast-glb.mjs` - authored hero features for Aldermoor Vale's pack predator: a raised dorsal hackle crest, a lateral shoulder ruff with a throat mass, a supraorbital shelf and a dark nose pad with nostril lobes. Built from three.js primitives by this repository's own script, positioned against landmarks derived at build time from `BeastBody.PROFILES.wolf` rather than hard-coded. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/beast-assets.test.mjs` asserts. | 2026-08-23 |
| `bear` | `public/assets/medieval/bear.glb` | `generated` | `scripts/make-beast-glb.mjs` - the same four features authored for the vale's solitary predator, with the crest over the HUMP rather than the withers (a bear's tallest point is not its shoulder blade), a shallower ruff, a heavier brow and a much larger nose pad. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `sec-b1` | `public/assets/dock/sec-b1.glb` | `generated` | `scripts/make-yard-glb.mjs` - Lodestar Yard's forward hull section on the jig at (-30, 22): twenty-four lapped strakes over five flanged ring frames, a bolted string course, a transverse bulkhead at the joined end, and a cut end opening on a collar frame and eight stringers with a seeded torn rim. Every mesh is named for a YARD MATERIAL KEY, so the geometry merges into a bucket `DockWorld` already draws and adds no draw call, material or shader program. Built from arithmetic by this repository's own script, dimensioned at build time from `dock/YardPlan.SECTIONS` rather than hard-coded. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/yard-assets.test.mjs` asserts. | 2026-08-23 |
| `sec-b2` | `public/assets/dock/sec-b2.glb` | `generated` | `scripts/make-yard-glb.mjs` - the same build at 8.8 m across and 16 m long over six bays, the largest single object on the yard floor. Same script, same provenance, same byte-diff gate; its rim tears on its own seed. | 2026-08-23 |
| `sec-b3` | `public/assets/dock/sec-b3.glb` | `generated` | `scripts/make-yard-glb.mjs` - berth three's midships section, 6.8 m across and 13 m long over five bays. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `reach-boulder` | `public/assets/space/reach-boulder.glb` | `generated` | `scripts/make-belt-glb.mjs` - the hero asteroid for Halberd Reach, the debris field in open space: a unit-radius boulder shaped by four octaves of deterministic value noise, dug into by nine crater bowls with raised ejecta rims, and sheared by three seeded fracture planes because a rock in a DEBRIS field is a fragment of something larger. Drawn by the 44 rocks at or above `Belt.HERO_RADIUS`, which is the same threshold the collider set uses - every rock the flight model can hit is a rock drawn at hero detail. The mesh is named for the belt's one MATERIAL KEY, so it shares the `MeshStandardMaterial` the three procedural silhouettes already use and adds no material and no shader program. Built from arithmetic by this repository's own script, tessellated at build time from the detail the loader publishes rather than hard-coded. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/belt-assets.test.mjs` asserts. | 2026-08-23 |
| `standing` | `public/assets/station/standing.glb` | `generated` | `scripts/make-crowd-glb.mjs` - authored hero features for Aether Nexus Station's ambient plaza crowd: a pair of hands, a hair cap with an occipital mass and a fringe edge, a coat collar with lapels, and a pair of shoes with a raked toe and a set-back heel. Built from three.js primitives by this repository's own script, positioned against joints derived at build time from `CROWD` in `station/StationKit.js` - the same table `StationWorld` builds the figure from - rather than hard-coded. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/crowd-assets.test.mjs` asserts. | 2026-08-23 |
| `seated` | `public/assets/station/seated.glb` | `generated` | `scripts/make-crowd-glb.mjs` - the same four features authored for the bench variant, whose skull sits 0.38 m lower and whose forearms rest on its thighs at a rake of 0.55 rad. Its own file rather than a translation of the standing one: only the hair moves rigidly with the head. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `citadel` | `public/assets/citadel/citadel.glb` | `generated` | `scripts/make-citadel-glb.mjs` - authored hero architecture for Sunspire Citadel: a two-centre pointed-arch surround (spandrels, archivolt moulding, keystone), a mashrabiya window screen (framed lattice with a pointed head), and a muqarnas corbel bracket course (two staggered tiers on a backing panel). Built from explicit quads by this repository's own script - there is no primitive for a curved void, which is exactly why these three and not others. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/citadel-assets.test.mjs` asserts, along with every normal being unit length - a zero-length one is valid glTF and NaN in a shader, and it whited out the whole gatehouse through bloom before that gate existed. | 2026-08-23 |
| `newel-finial` | `public/assets/maze/newel-finial.glb` | `generated` | `scripts/make-newel-glb.mjs` - procedural turned newel post (plinth, coved shaft, collar bead, acorn finial), authored by this repository's own script against three.js `LatheGeometry`. No external source, no generator service, nobody owed attribution. Re-running the script reproduces the file. | 2026-08-10 |
| `leaf-tuft` | `public/assets/maze/leaf-tuft.glb` | `generated` | `scripts/make-maze-glb.mjs` - the hedge-top sprig and the shaft ivy leaf, one geometry: an irregular five-fold bipyramidal tuft with a zigzag equator and a leaning tip, authored to exactly the 0.5 m bounding cube of the `BoxGeometry(0.5, 0.5, 0.5)` it replaces. TEN triangles against the box's twelve, because this is the most-instanced geometry in the game - 3,600 per district, 21 districts resident at the maze entrance. Built from arithmetic by this repository's own script; the generator refuses to write a file with a degenerate normal, an unmatched directed edge or a negative signed volume, and `scripts/tests/maze-glb.test.mjs` re-asserts all three against the committed bytes and reproduces the file byte for byte. No external source, no generator service, no scan, nobody owed attribution. | 2026-08-23 |
| `hedge-candle` | `public/assets/maze/hedge-candle.glb` | `generated` | `scripts/make-maze-glb.mjs` - the corridor candle: a seven-sided wax pillar with a melted lip and a flame, authored inside the candle descriptor's own 0.18 x 0.52 x 0.18 dressing box so `MazeMeshes.buildAssetPrefab` refits it at scale 1.0. Seven sides rather than eight because an even ring presents a flat face square-on to a corridor that runs on the same axes the hedges do, which is what the box it replaces already looked like. Same script, same provenance, same three geometry gates, same byte-diff. | 2026-08-23 |
| `surf-hedge-albedo`, `surf-hedge-normal`, `surf-hedge-orm` | `public/assets/maze/tex/hedge-*.ktx2` | `CC0-1.0` | ambientCG **Moss002** (<https://ambientcg.com/view?id=Moss002>), fetched as <https://ambientcg.com/get?file=Moss002_2K-JPG.zip>. Colour, NormalGL, AmbientOcclusion and Roughness maps; ORM packed offline to the glTF convention (R=AO, G=roughness, B=metalness, metal=0) and the set compressed to KTX2 at 2048px (ETC1S for albedo/ORM, UASTC for the normal map). No attribution owed; recorded because every external file gets a line. | 2026-08-10 |
| `surf-floor-albedo`, `surf-floor-normal`, `surf-floor-orm` | `public/assets/maze/tex/floor-*.ktx2` | `CC0-1.0` | Poly Haven **Dirt Floor** (<https://polyhaven.com/a/dirt_floor>), fetched as `dirt_floor_{diff,nor_gl,arm}_2k.jpg` from <https://dl.polyhaven.org/file/ph-assets/Textures/jpg/2k/dirt_floor/>. Poly Haven's `arm` map is already the glTF ORM layout; compressed to KTX2 at 2048px (ETC1S albedo/ORM, UASTC normal). Physical scale 2.07m per tile, per the asset's own metadata. | 2026-08-10 |
| `surf-stair-albedo`, `surf-stair-normal`, `surf-stair-orm` | `public/assets/maze/tex/stair-*.ktx2` | `CC0-1.0` | ambientCG **Travertine003** (<https://ambientcg.com/view?id=Travertine003>), fetched as <https://ambientcg.com/get?file=Travertine003_2K-JPG.zip>. Pale banded travertine for the stair/shaft-wall stonework; ORM packed offline (metal=0), downsampled to 1024px before KTX2 compression - the stair budget class was 512px procedural, and 1024 authored is already a step up. | 2026-08-10 |
| `surf-footing-albedo`, `surf-footing-normal`, `surf-footing-orm` | `public/assets/maze/tex/footing-*.ktx2` | `CC0-1.0` | Poly Haven **Castle Wall Slates** (<https://polyhaven.com/a/castle_wall_slates>), fetched as `castle_wall_slates_{diff,nor_gl,arm}_2k.jpg` from <https://dl.polyhaven.org/file/ph-assets/Textures/jpg/2k/castle_wall_slates/>. Weathered stacked stone courses for the hedge footings; downsampled to 1024px before KTX2 compression. Physical scale 2.5m per tile, per the asset's own metadata. | 2026-08-10 |
| `surf-tunnel-albedo`, `surf-tunnel-normal`, `surf-tunnel-orm` | `public/assets/maze/tex/tunnel-*.ktx2` | `CC0-1.0` | Poly Haven **Park Dirt** (<https://polyhaven.com/a/park_dirt>), fetched as `park_dirt_{diff,nor_gl,arm}_2k.jpg` from <https://dl.polyhaven.org/file/ph-assets/Textures/jpg/2k/park_dirt/>. Warm packed dirt with fine debris for the tunnel treads; downsampled to 1024px before KTX2 compression. Physical scale 3m per tile, per the asset's own metadata. | 2026-08-10 |
| `crowd-stand` | `public/assets/sports/crowd-stand.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - Meridian Athletic Grounds' 132 standing figures: a hair cap with a stretched occipital mass and a hard fringe bar across the brow, a pair of hands, and a pair of raked shoes with a toe overhang. Built from three.js primitives by this repository's own script, positioned at build time against the limb they attach to in `sports/CrowdKit.js` - the same table `SportsWorld` builds the figure from - rather than hard-coded. Every mesh names one of the two surfaces the crowd already draws, so the geometry merges into an `InstancedMesh` the world already has and adds no draw call, material or shader program. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/sports-crowd-assets.test.mjs` asserts. | 2026-08-23 |
| `crowd-sit` | `public/assets/sports/crowd-sit.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features authored for the 382 spectators in the four stands - by a factor of three the largest population in this world. Its shins are vertical and end on a bleacher tread, so its shoes sit where a standing figure's do; its forearms rake forward onto its thighs at 0.7 rad, so its hands do not. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `crowd-lean` | `public/assets/sports/crowd-lean.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features for the 42 figures standing with their weight on one hip along the barriers and fence lines. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `crowd-carry` | `public/assets/sports/crowd-carry.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features for the 19 figures arriving with kit. Its hands are placed in the ARM's own frame rather than at a typed point, which is what caught the pose's left arm having been rotated into a salute with its hand end at the centre of the chest. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `crowd-crouch` | `public/assets/sports/crowd-crouch.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features for the 8 figures squatting on the coping and the starting blocks. The only pose whose limbs are not rebuilt open-ended, because its thigh and shin end faces are 30 cm apart and its shoulders sit clear of its torso, so those caps are genuinely visible. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `ejecta-block` | `public/assets/planets/ejecta-block.glb` | `generated` | `scripts/make-planet-glb.mjs` - the rock every planet surface is scattered with: a unit-radius ejecta block shaped by three octaves of deterministic value noise biased inward so the body has bays rather than bulges, and asymmetrised by two seeded clipping planes on non-opposed axes - which at twenty triangles buy lopsidedness rather than a fracture face, because a 37-degree cap cannot contain two of twelve vertices 63.4 degrees apart, a limitation the test pins with a negative assertion. It replaces `IcosahedronGeometry(1, 0)` at EXACTLY its twenty triangles, inside the field's existing `InstancedMesh`, so it adds no renderable, no instanced mesh, no draw call, no material, no shader program and no collider - the strongest budget position any asset in this repository has taken. Which boulders use it is the descriptor's own word for what the thing is, `kind: 'boulders'`: all 8,880 across thirteen fields on nine of the ten planets. Every face carries its own UV island at a seeded offset AND a seeded rotation, replacing the polyhedron's spherical UVs, which pinch at the poles and seam down the lit face. Built from arithmetic by this repository's own script; the generator refuses to write a file with a degenerate normal, an unmatched directed edge, a negative signed volume or a vertex outside the envelope the primitive reached, and `scripts/tests/planet-assets.test.mjs` re-asserts all four against the committed bytes and reproduces the file byte for byte. No external source, no generator service, no scan, nobody owed attribution. | 2026-08-24 |
| `spectator` | `public/assets/race/spectator.glb` | `generated` | `scripts/make-race-glb.mjs` - the grandstand figure for Vellum Ridge, Cinder Gorge and Aurora Rise: a six-sided trunk with a waist, two four-sided arms hanging clear of the flank so daylight shows between arm and body, a four-sided skull that is WIDEST AT THE BROW because a skull tapers upward and a sphere cannot, a nose ridge authored as a closed triangular prism, and two legs. It replaces `sweep(...) + blob(...)` at EXACTLY its 144 triangles, inside the `race:crowd` `InstancedMesh` the world already builds, so it adds no renderable, no instanced mesh, no draw call and no shader program - and it takes the world's material count DOWN by one, because carrying a per-part shade needs `vertexColors` and the crowd's `paint.enamel|novc` clone was the only material in the scene with that configuration. The shades are what fix the defect it exists for: body and head used to be one geometry drawn with `vertexColors: false` and handed one `setColorAt`, so the head was painted the shirt colour, on all 819 figures. Built from arithmetic by this repository's own script, dimensioned at build time from the part shades and triangle budget `src/worlds/race/RaceAssets.js` publishes rather than hard-coded. The generator refuses to write a file with a degenerate face, an unmatched directed edge or a negative signed volume, PER SOLID - the inherited version tallied edges across the whole part and claimed that abutting solids were still each closed, which is false and which its own gate proved on the first run. `scripts/tests/race-assets.test.mjs` re-asserts all three against the committed bytes and reproduces the file byte for byte. No external source, no generator service, no scan, nobody owed attribution. | 2026-08-24 |
| `marshal` | `public/assets/race/marshal.glb` | `generated` | `scripts/make-race-glb.mjs` - the flag point, 29 of them round the three circuits: a recessed observation slot behind a surround of sill, head, two jambs and a mullion standing 0.14 m proud, a working platform with two kick rails, a roof with a real drip edge under its overhang, a grab rail down each flank, and a hazard band of four bars standing 0.06 m proud on ALL FOUR faces. That band is the point. The three boxes it replaces drew a band 0.2 m deep at the centre of a solid 2.6 m deep, so 93% of its surface was buried and the 7% that rendered was a 5 cm tab on each END, side-on to the road - the citadel's window recesses again, arithmetic that reads perfectly and has never produced a pixel a driver could see. Every mesh is NAMED FOR A RACE MATERIAL KEY, so the geometry merges into the `trackside.<id>` `Batch` bucket of that key and adds no draw call, material or shader program. 204 triangles against 132, which is +2,088 across 29 posts and +0.03% of a measured frame, paid on the record and affordable only because 108 of the 132 it replaces are the bevel on one `RoundedBoxGeometry`. The SHELL is unchanged at 3.2 x 2.6 x 2.6 on the same origin, because `_buildTrackside` puts a collider on the shell and art does not move collision. Same script, same provenance, same three per-solid gates, same byte-diff. | 2026-08-24 |
| `frame` | `public/assets/medieval/frame.glb` | `generated` | `scripts/make-medieval-glb.mjs` - authored timber-frame carpentry for Aldermoor Vale, the environment half of the medieval art pass: a curved arch-brace (a cosine-bowed blade that deepens through the belly, 0.17 m at the ends and 0.29 m at the crook, replacing the straight strap in every storey corner of `MedievalWorld._house`) and a cyma jetty console (carried under each joist end of a jettied storey, which used to end on bare box ends). Built from explicit quads by this repository's own script - a curve is the one shape the village's box batch cannot make, which is exactly why these two and not others. Both parts bind to the `beam` material slot the village GeoBatch already flushes and are MERGED into that bucket, so they add no draw call, no material and no shader program; the cost is 24,968 triangles over 424 braces and 154 consoles, priced by a twice-built headless village in `scripts/tests/medieval-frame-assets.test.mjs`. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which that test asserts, along with every normal being unit length in the committed bytes. | 2026-08-26 |
