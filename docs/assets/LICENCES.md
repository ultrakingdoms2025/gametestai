# Asset licence ledger

Every external or authored file under `public/assets/` gets a line here on the
day it lands, whether or not its licence carries an attribution obligation.
`scripts/tests/maze-assets.test.mjs` enforces both halves: every manifest
entry's licence must come from the allow-list below, and every entry's id must
appear in this file.

## Allow-list

| Licence | Obligation |
|---|---|
| `CC0-1.0` | None. Preferred for anything fetched from outside. |
| `CC-BY-4.0` | Attribution REQUIRED - the line here must name the author and source URL. |
| `proprietary-owned` | None - the project owns it outright. |
| `generated` | None - produced by a script in this repository (or a generator whose terms are recorded on its line). |

Anything else - Sketchfab's per-model terms especially - is not accepted. If a
model is worth an exception, the exception goes through the allow-list in the
test, deliberately, in its own commit.

## Ledger

| id | file | licence | source | fetched/made |
|---|---|---|---|---|
| `raider` | `public/assets/npc/raider.glb` | `generated` | `scripts/make-npc-glb.mjs` - authored hero features for the station's four attacker archetypes: crested cranium, brow shelf, open muzzle with fangs, ears, glowing eye caps, pauldron, bandolier, dorsal and forearm spikes, belt and knuckled hands. Built from three.js primitives by this repository's own script against the reference sheets in `demopics/` (`g1`-`g4`). No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/npc-assets.test.mjs` asserts. | 2026-08-23 |
| `crew` | `public/assets/npc/crew.glb` | `generated` | `scripts/make-npc-glb.mjs` - authored hero features for the station's seven fixed interactive roles: gorilla cranium, brow, closed muzzle, ears, EVA collar ring, life-support pack, chest rig with a lit panel, belt, boots and bare knuckled hands. Same script, same provenance, authored against `demopics/n1`-`n7`. The helmet bubble those references wear is deliberately absent: a transparent material is a new shader-program family, and this pipeline exists to avoid exactly that. | 2026-08-23 |
| `wolf` | `public/assets/medieval/wolf.glb` | `generated` | `scripts/make-beast-glb.mjs` - authored hero features for Aldermoor Vale's pack predator: a raised dorsal hackle crest, a lateral shoulder ruff with a throat mass, a supraorbital shelf and a dark nose pad with nostril lobes. Built from three.js primitives by this repository's own script, positioned against landmarks derived at build time from `BeastBody.PROFILES.wolf` rather than hard-coded. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/beast-assets.test.mjs` asserts. | 2026-08-23 |
| `bear` | `public/assets/medieval/bear.glb` | `generated` | `scripts/make-beast-glb.mjs` - the same four features authored for the vale's solitary predator, with the crest over the HUMP rather than the withers (a bear's tallest point is not its shoulder blade), a shallower ruff, a heavier brow and a much larger nose pad. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `sec-b1` | `public/assets/dock/sec-b1.glb` | `generated` | `scripts/make-yard-glb.mjs` - Lodestar Yard's forward hull section on the jig at (-30, 22): twenty-four lapped strakes over five flanged ring frames, a bolted string course, a transverse bulkhead at the joined end, and a cut end opening on a collar frame and eight stringers with a seeded torn rim. Every mesh is named for a YARD MATERIAL KEY, so the geometry merges into a bucket `DockWorld` already draws and adds no draw call, material or shader program. Built from arithmetic by this repository's own script, dimensioned at build time from `dock/YardPlan.SECTIONS` rather than hard-coded. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/yard-assets.test.mjs` asserts. | 2026-08-23 |
| `sec-b2` | `public/assets/dock/sec-b2.glb` | `generated` | `scripts/make-yard-glb.mjs` - the same build at 8.8 m across and 16 m long over six bays, the largest single object on the yard floor. Same script, same provenance, same byte-diff gate; its rim tears on its own seed. | 2026-08-23 |
| `sec-b3` | `public/assets/dock/sec-b3.glb` | `generated` | `scripts/make-yard-glb.mjs` - berth three's midships section, 6.8 m across and 13 m long over five bays. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `standing` | `public/assets/station/standing.glb` | `generated` | `scripts/make-crowd-glb.mjs` - authored hero features for Aether Nexus Station's ambient plaza crowd: a pair of hands, a hair cap with an occipital mass and a fringe edge, a coat collar with lapels, and a pair of shoes with a raked toe and a set-back heel. Built from three.js primitives by this repository's own script, positioned against joints derived at build time from `CROWD` in `station/StationKit.js` - the same table `StationWorld` builds the figure from - rather than hard-coded. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/crowd-assets.test.mjs` asserts. | 2026-08-23 |
| `seated` | `public/assets/station/seated.glb` | `generated` | `scripts/make-crowd-glb.mjs` - the same four features authored for the bench variant, whose skull sits 0.38 m lower and whose forearms rest on its thighs at a rake of 0.55 rad. Its own file rather than a translation of the standing one: only the hair moves rigidly with the head. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `newel-finial` | `public/assets/maze/newel-finial.glb` | `generated` | `scripts/make-newel-glb.mjs` - procedural turned newel post (plinth, coved shaft, collar bead, acorn finial), authored by this repository's own script against three.js `LatheGeometry`. No external source, no generator service, nobody owed attribution. Re-running the script reproduces the file. | 2026-08-10 |
| `surf-hedge-albedo`, `surf-hedge-normal`, `surf-hedge-orm` | `public/assets/maze/tex/hedge-*.ktx2` | `CC0-1.0` | ambientCG **Moss002** (<https://ambientcg.com/view?id=Moss002>), fetched as <https://ambientcg.com/get?file=Moss002_2K-JPG.zip>. Colour, NormalGL, AmbientOcclusion and Roughness maps; ORM packed offline to the glTF convention (R=AO, G=roughness, B=metalness, metal=0) and the set compressed to KTX2 at 2048px (ETC1S for albedo/ORM, UASTC for the normal map). No attribution owed; recorded because every external file gets a line. | 2026-08-10 |
| `surf-floor-albedo`, `surf-floor-normal`, `surf-floor-orm` | `public/assets/maze/tex/floor-*.ktx2` | `CC0-1.0` | Poly Haven **Dirt Floor** (<https://polyhaven.com/a/dirt_floor>), fetched as `dirt_floor_{diff,nor_gl,arm}_2k.jpg` from <https://dl.polyhaven.org/file/ph-assets/Textures/jpg/2k/dirt_floor/>. Poly Haven's `arm` map is already the glTF ORM layout; compressed to KTX2 at 2048px (ETC1S albedo/ORM, UASTC normal). Physical scale 2.07m per tile, per the asset's own metadata. | 2026-08-10 |
| `surf-stair-albedo`, `surf-stair-normal`, `surf-stair-orm` | `public/assets/maze/tex/stair-*.ktx2` | `CC0-1.0` | ambientCG **Travertine003** (<https://ambientcg.com/view?id=Travertine003>), fetched as <https://ambientcg.com/get?file=Travertine003_2K-JPG.zip>. Pale banded travertine for the stair/shaft-wall stonework; ORM packed offline (metal=0), downsampled to 1024px before KTX2 compression - the stair budget class was 512px procedural, and 1024 authored is already a step up. | 2026-08-10 |
| `surf-footing-albedo`, `surf-footing-normal`, `surf-footing-orm` | `public/assets/maze/tex/footing-*.ktx2` | `CC0-1.0` | Poly Haven **Castle Wall Slates** (<https://polyhaven.com/a/castle_wall_slates>), fetched as `castle_wall_slates_{diff,nor_gl,arm}_2k.jpg` from <https://dl.polyhaven.org/file/ph-assets/Textures/jpg/2k/castle_wall_slates/>. Weathered stacked stone courses for the hedge footings; downsampled to 1024px before KTX2 compression. Physical scale 2.5m per tile, per the asset's own metadata. | 2026-08-10 |
| `surf-tunnel-albedo`, `surf-tunnel-normal`, `surf-tunnel-orm` | `public/assets/maze/tex/tunnel-*.ktx2` | `CC0-1.0` | Poly Haven **Park Dirt** (<https://polyhaven.com/a/park_dirt>), fetched as `park_dirt_{diff,nor_gl,arm}_2k.jpg` from <https://dl.polyhaven.org/file/ph-assets/Textures/jpg/2k/park_dirt/>. Warm packed dirt with fine debris for the tunnel treads; downsampled to 1024px before KTX2 compression. Physical scale 3m per tile, per the asset's own metadata. | 2026-08-10 |
| `crowd-stand` | `public/assets/sports/crowd-stand.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - Meridian Athletic Grounds' 132 standing figures: a hair cap with a stretched occipital mass and a hard fringe bar across the brow, a pair of hands, and a pair of raked shoes with a toe overhang. Built from three.js primitives by this repository's own script, positioned at build time against the limb they attach to in `sports/CrowdKit.js` - the same table `SportsWorld` builds the figure from - rather than hard-coded. Every mesh names one of the two surfaces the crowd already draws, so the geometry merges into an `InstancedMesh` the world already has and adds no draw call, material or shader program. No external source, no generator service, no scan, nobody owed attribution. Re-running the script reproduces the file byte for byte, which `scripts/tests/sports-crowd-assets.test.mjs` asserts. | 2026-08-23 |
| `crowd-sit` | `public/assets/sports/crowd-sit.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features authored for the 382 spectators in the four stands - by a factor of three the largest population in this world. Its shins are vertical and end on a bleacher tread, so its shoes sit where a standing figure's do; its forearms rake forward onto its thighs at 0.7 rad, so its hands do not. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `crowd-lean` | `public/assets/sports/crowd-lean.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features for the 42 figures standing with their weight on one hip along the barriers and fence lines. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `crowd-carry` | `public/assets/sports/crowd-carry.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features for the 19 figures arriving with kit. Its hands are placed in the ARM's own frame rather than at a typed point, which is what caught the pose's left arm having been rotated into a salute with its hand end at the centre of the chest. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
| `crowd-crouch` | `public/assets/sports/crowd-crouch.glb` | `generated` | `scripts/make-sports-crowd-glb.mjs` - The same three features for the 8 figures squatting on the coping and the starting blocks. The only pose whose limbs are not rebuilt open-ended, because its thigh and shin end faces are 30 cm apart and its shoulders sit clear of its torso, so those caps are genuinely visible. Same script, same provenance, same byte-diff gate. | 2026-08-23 |
